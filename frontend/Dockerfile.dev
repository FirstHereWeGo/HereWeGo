# frontend/Dockerfile.dev (개발용 - 볼륨 마운트 + hot reload)

FROM node:20-slim
WORKDIR /app

COPY package.json package-lock.json ./
RUN npm install

# 소스코드는 COPY하지 않음 - docker-compose.dev.yml의 volumes가 대신 마운트함

EXPOSE 5173
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]
