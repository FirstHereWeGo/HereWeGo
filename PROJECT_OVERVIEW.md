# PRIME REWIND — 프로젝트 개요 (기획서용 참고자료)

> 이 문서는 2026-07-21 기준 실제 코드 상태를 읽고 검증해 작성했다. 기획서(제안서/발표자료) 본문을 쓸 때 그대로 가져다 쓰거나 각색할 수 있도록 "무엇을 만들고 있는가"를 중심으로 정리했다.
> 구현 세부(API 스펙, 스키마 필드 등)는 [setting.md](setting.md)를 참조. 이 문서는 그보다 상위 레벨의 "제품/기획" 관점 설명이다.

---

## 1. 한 줄 소개

**2026 월드컵 국가대표팀을 골라 포메이션과 전술을 직접 짜보고, 그 조합이 상대 팀 대비 얼마나 이길 확률이 있는지 예측해보는 웹 기반 전술 시뮬레이터.**

랜딩 화면의 카피가 이 프로젝트의 정체성을 그대로 보여준다:

> "내 팀과 상대 팀을 고르고, 포메이션과 전술을 직접 구성해보세요. 라인업·교체·전술 성향을 바꾼 뒤 **예측 승률**을 확인할 수 있습니다."

내부 코드네임 겸 브랜드명은 **PRIME REWIND**(package.json 상 `prime-rewind`)이며, 랜딩 화면 이거비(eyebrow)에는 "TACTICS PREDICTOR"라는 카테고리 문구가 붙어 있다.

## 2. 배경 / 소재

- 시즌 소재는 **2026 FIFA 월드컵**. 현재 seed 데이터로 **대한민국(kor)**과 **남아프리카공화국(rsa)** 두 팀의 실제 월드컵 명단(대한민국 27인조, 남아공 40인조 PDF 기준)을 실명·포지션·생년월일 기반 나이/키까지 반영해 넣어뒀다.
- 세부 능력치(스탯 10종, 양발 능력치)는 아직 실제 스카우팅 값이 아니라 placeholder(전부 동일값)로 채워진 상태 — 이 부분이 이후 "실제 능력치 반영" 확장 포인트다.
- 축구 매니지먼트 게임(Football Manager 등)의 전술판을 참고해, `TacticConfig`가 FM 스타일 5개 섹션(스타일/공격 시 빌드업/상대 진영 공략/전환/비공격 시 수비)을 그대로 미러링한다. 즉 "간단한 포메이션 선택"이 아니라 세부 전술 다이얼을 조작하는 수준의 깊이를 목표로 한다.

## 3. 핵심 사용자 흐름 (실제 화면 전환)

```
랜딩(Landing)
   → 팀 선택(TeamSelect): 내 팀 / 상대 팀 각각 선택 (같은 팀 중복 선택 방지, 클릭 시 슬롯 스왑 지원)
   → 전술 보드(TacticalBoard)
        ├─ [포메이션] 탭: 3D 피치 위에 라인업 배치(PitchBoard, Three.js) + 선수 카드 + 벤치 목록
        ├─ [전술] 탭: FM식 상세 전술 폼(TacticsPanel) — 5개 섹션 전체 노출
        └─ "승률 계산" 버튼: 현재 라인업/전술 스냅샷을 backend로 보내 승/무/패 확률을 받아 상단에 바 형태로 표시
```

- 승률 계산은 **버튼을 눌렀을 때만** 1회성으로 실행된다. 실시간 경기 시뮬레이션이 아니라 **프리매치(경기 전) 스쿼드·전술 편집 도구**라는 점이 설계상 명확한 제약이자 컨셉이다.
- 3D 피치(`frontend/src/three/PitchScene.js`)는 실제 축구장 규격(105m×68m)을 반영한 텍스처와 브로드캐스트/탑뷰 카메라 프리셋을 갖춘 Three.js 씬으로, 선수를 실물 대비 확대된 스케일(약 2.5배)로 배치해 시각적으로 조작하기 쉽게 만든 것이 특징이다.

## 4. 제품 차별점 (기획서에 쓸 만한 포인트)

1. **국가대표팀 실명 로스터 기반** — 가상 팀이 아니라 실제 2026 월드컵 출전 선수 명단으로 시뮬레이션한다는 점에서 몰입감/화제성이 있다.
2. **FM 수준의 전술 디테일** — 단순 포메이션 스티커가 아니라, 빌드업 스타일/템포/압박 라인/오프사이드 트랩 등 세부 다이얼을 실제로 조작 가능.
3. **3D 인터랙티브 피치** — 2D 도식이 아닌 3D 렌더링으로 라인업을 배치·확인.
4. **승률 예측이라는 "결과 피드백 루프"** — 전술을 바꾸는 행위가 즉각적인 정량적 피드백(승/무/패 %)으로 이어져, "이렇게 짜면 더 유리할까?"를 실험해보는 게임성을 가진다.
5. **계산-데이터 책임 분리 아키텍처** — backend(데이터/조회)와 model(순수 계산)을 완전히 분리해, 이후 머신러닝 모델로 교체할 여지를 처음부터 설계에 반영해뒀다(아래 6장).

## 5. 현재 구현 상태 (기획서에 "현재 vs 목표"를 나눠 쓸 때 참고)

| 영역 | 상태 | 비고 |
|---|---|---|
| 팀/로스터 조회 | ✅ 구현 | 한국/남아공 실명 로스터. 능력치는 placeholder |
| 포메이션 카탈로그 | ✅ 구현 | 백3/4/5 기반 22종 |
| 전술 설정 UI | ✅ 구현 | FM 5섹션 풀 폼 |
| 3D 피치 라인업 배치 | ✅ 구현 | 드래그/배치, 벤치 교체 등 |
| **승률 예측** (`/api/win-probability`) | ⚠️ 휴리스틱으로 동작 | 학습된 ML 모델이 아니라 손튜닝 가중치 기반 rating 계산(스탯 합산 + 포지션 미스매치/연령/신장 페널티 + 전술 보정) → 승/무/패 확률로 변환. "정식 모델 도입 전 임시 구현"이 코드 주석에 명시돼 있음 |
| xG(장면별 실점확률) `/api/xg/rewind` | ❌ 스텁 | 항상 `NotImplementedError` — 프론트에 노출된 기능 없음 |
| xG(레거시 스칼라) `/api/xg` | ✅ 구현 | 슈터 위치·수비수 수 기반 손공식, 하위호환용 |
| 선수 프리셋 / 골 시나리오 데이터 | ❌ 비어있음 | 스키마·라우터는 있으나 seed 데이터 없음 |
| 실제 선수 능력치(스카우팅 데이터) | ❌ 미반영 | 전원 placeholder 스탯 |

## 6. 향후 확장 방향 (제안서의 "로드맵" 섹션용)

- **승률 예측 모델 고도화**: 현재 휴리스틱 rating을 실제 학습 데이터로 대체 — `model/app/predictors/win_predictor/win_predictor.py`에 이미 "학습된 모델이 준비되면 이 함수를 모델 로드+추론으로 교체" 하도록 자리가 마련돼 있음.
- **xG 리와인드 기능 완성**: 특정 장면(슈터/수비수/GK 위치 + 수비 전술)을 재현해 실점확률을 보여주는 기능 — 스키마는 있으나 모델 로직 미구현.
- **선수 실제 능력치 반영**: 현재 placeholder인 10종 스탯/양발 능력치를 실제 스카우팅 값으로 교체하면 팀 간 실력 차이가 승률에 훨씬 정교하게 반영됨.
- **팀 확장**: 현재 2개국(한국/남아공)뿐 — 월드컵 참가국 추가.
- **선수 프리셋/골 시나리오**: 비어있는 `player_presets`, `goal_scenarios` 데이터를 채워 "가상 유망주 끼워보기", "특정 골 장면 리플레이" 같은 부가 기능으로 확장 가능.

## 7. 시스템 구조 요약

3-컨테이너 구조(frontend / backend / model)로 역할을 엄격히 분리한다 — frontend는 backend만 호출하고, backend는 데이터 조회·정제와 model 프록시만 담당하며(계산 로직 없음), model은 저장소 없이 매 요청의 JSON만으로 계산한다. 상세 스펙(엔드포인트, 스키마, 포트, Docker 구성)은 [setting.md](setting.md) 참조.

## 8. 기술 스택

- **Frontend**: React 19 + Vite + Three.js (순수 CSR SPA, Next.js 아님 — 루트 `README.md`는 과거 Next.js 기준이라 이 부분이 최신 상태와 다름)
- **Backend / Model**: FastAPI(Python), 완전히 분리된 두 서비스
- **배포**: Docker Compose (`docker-compose.yml` prod / `docker-compose.dev.yml` dev), 공유 네트워크 `herewego-net`
