# PRIME REWIND (HereWeGo) — 서비스 기획서

> 작성 기준일: 2026-07-25 / `dev` 브랜치 실제 구현 상태를 코드 기반으로 검증해 작성.
> 이 문서는 `PROJECT_OVERVIEW.md`(2026-07-21 스냅샷)보다 이후에 추가된 토너먼트·분석·AI 코치·3D 감독 연출까지 포함한 최신 기획 문서다. API/스키마 등 구현 세부는 `setting.md`를 참조.

---

## 1. 서비스 개요

### 1.1 한 줄 소개

**2026 FIFA 월드컵을 배경으로, 실제 국가대표팀의 감독이 되어 라인업·전술을 직접 설계하고, 그 선택이 승률에 어떤 영향을 주는지 즉시 확인하며 토너먼트를 치르는 웹 기반 축구 전술 시뮬레이터.**

랜딩 화면 카피가 제품 정체성을 요약한다:

> "내 팀과 상대 팀을 고르고, 포메이션과 전술을 직접 구성해보세요. 라인업·교체·전술 성향을 바꾼 뒤 예측 승률을 확인할 수 있습니다."

내부 브랜드명은 **PRIME REWIND**(`prime-rewind`), 랜딩의 이거비 문구는 **"TACTICS PREDICTOR"**.

### 1.2 배경 소재

- 시즌 소재는 2026 FIFA 월드컵. 현재 대한민국(kor, 감독 홍명보)·남아프리카공화국(rsa, 감독 Hugo Broos) 두 팀이 실제 공식 명단(이름·포지션·나이·키) 기준으로 시딩되어 있고, 손흥민·메시·호날두 등 "전성기 레전드 선수"를 별도 선수로 소환해 실전 라인업에 끼워 넣을 수 있다.
- 전술 설계는 Football Manager의 전술판을 참고해 5개 섹션(스타일 / 공격 시 빌드업 / 상대 진영 공략 / 전환 / 비공격 시 수비)을 그대로 미러링했다. 즉 "포메이션 스티커 선택" 수준이 아니라, 압박 강도·라인 높이·템포·오프사이드 트랩 같은 세부 다이얼을 실제로 조작하는 깊이를 목표로 한다.
- 화면 전체가 데이터 대시보드에 가까운 다크 글래스모피즘 톤 + BBC 스포츠식 "짙은 무채색 스튜디오에 피치만 떠 있는" 3D 연출로, "게임 필드"보다 "방송사급 전술 분석 콘솔"에 가까운 룩앤필을 지향한다.

### 1.3 제품 차별점

1. **국가대표팀 실명 로스터 기반** — 가상 팀이 아닌 실제 2026 월드컵 출전 선수/실제 감독으로 시뮬레이션.
2. **FM 수준의 전술 디테일** — 포메이션뿐 아니라 5개 섹션의 세부 전술 다이얼을 실제로 조작 가능.
3. **3D 인터랙티브 피치 + 감독 시점 카메라** — 방송 중계 카메라, 탑뷰뿐 아니라 더그아웃에 선 감독의 눈높이에서 경기장을 바라보는 1인칭 뷰까지 제공.
4. **승률 예측이라는 즉각적 피드백 루프** — 전술을 바꾸는 행위가 곧바로 승/무/패 %와 "전술적 결함" 진단으로 돌아와, "이렇게 짜면 더 유리한가?"를 실험하는 게임성을 만든다.
5. **AI 전술 코치(Gemini 연동)** — 현재 라인업·전술·승률·전술적 결함을 종합해 한국어로 개선 제안을 해주는 어시스턴트.
6. **8강부터 결승까지 이어지는 토너먼트** — 단발성 승률 계산이 아니라, 라운드마다 전술을 다시 조정하며 우승까지 이어가는 서사 구조.
7. **계산-데이터 책임 분리 아키텍처** — frontend / backend(데이터 조회·정제) / model(순수 계산) 3계층 분리로, 지금의 손튜닝 휴리스틱을 이후 학습된 ML 모델로 교체할 여지를 처음부터 설계에 반영.

### 1.4 현재 구현 상태 요약

| 영역 | 상태 | 비고 |
|---|---|---|
| 팀 선택 · 로스터 조회 | ✅ | 한국·남아공 실명 로스터, 스탯 10종은 사람이 임의 배정(스카우팅 데이터 아님) |
| 포메이션 · 전술 설정 UI | ✅ | 백3/4/5 기반 22종 + FM 5섹션 풀 폼 |
| 3D 피치 라인업 배치 | ✅ | 클릭 스왑, 벤치/프라임 선수 교체, 카메라 3종 |
| 승률 예측 (`/api/win-probability`) | ✅ (휴리스틱) | 학습된 ML이 아니라 스탯 가중합 + 페널티 기반 rating → 확률 변환. 코드 주석에 "정식 모델 도입 전 임시 구현"이라 명시 |
| 토너먼트(8강~결승) | ✅ | 유저 경기는 구간별 라이브 진행 + 구간 사이 전술 재조정 허용 |
| 경기 통계 대시보드 / 모멘텀 차트 | ✅ | 전부 손튜닝 통계 생성기, 실제 이벤트 로그 아님 |
| AI 전술 제안(Gemini) | ✅ | 승률 계산 이후에만 활성화 |
| xG 리와인드(장면별 실점확률) | ❌ 스텁 | 스키마·엔드포인트는 있으나 미구현, 프론트 미노출 |
| 선수 프리셋 / 골 시나리오 seed | ❌ 비어있음 | 라우터만 존재 |
| 감독 유형 판정 + 공유 카드 | ❌ 미착수(설계 의도만 존재) | §2.4 참고 |

---

## 2. "감독 경험" 설계 의도

### 2.1 핵심 컨셉: "선수가 아니라 감독을 플레이한다"

이 제품은 사용자가 그라운드 위 22명 중 하나를 조작하는 게임(FIFA류)이 아니라, **경기 시작 전 스쿼드와 전술을 설계하고, 경기 중에는 정해진 구간마다 개입해 지시를 바꾸는 의사결정자**로 사용자를 위치시킨다. 이 결정 → 예측/진단 피드백 → 재조정 사이클 자체가 축구 감독의 실제 업무 흐름(스쿼드 파악 → 전형/지시 설계 → 데이터 기반 사전 검토 → 실전 투입 → 중간 점검/재조정)을 흉내 낸 구조이며, 화면·3D 연출·데이터 문구 전반이 이 "감독" 정체성을 반복해서 강화하도록 설계되어 있다.

### 2.2 "감독"임을 문자 그대로 보여주는 장치들

| 장치 | 위치 | 의도 |
|---|---|---|
| **실제 국가대표 감독 배정** | `backend/app/data/teams.py` — 한국 "홍명보", 남아공 "Hugo Broos" | 가상의 팀이 아니라 "실존하는 이 감독의 자리에 당신이 들어간다"는 몰입감 부여 |
| **더그아웃(기술 지역) 3D 오브젝트** | `three/PitchScene.js`의 `_buildTechnicalArea()` | 유리벽 더그아웃 + 정장을 입은 스태프 피규어를 팀별로 배치하고, 머리 위에 실제 감독 이름과 "감독" 라벨을 띄움 — 사용자가 조작하는 팀에 "감독이 이미 그 자리에 서 있다"는 시각적 증거를 제공 |
| **"감독 시점" 카메라 모드** | `PitchBoard.jsx` 카메라 버튼(중계 캠 / **감독 시점** / 탑뷰), `PitchScene.js`의 `managerFP` 모드 | 감독 피규어의 눈높이에 카메라를 고정하고 공을 향해 시선을 두는 1인칭 뷰(유휴 시 헤드밥 애니메이션까지) — "당신이 지금 이 더그아웃에 서 있다"를 가장 직접적으로 체감시키는 인터랙션 |
| **AI 코치의 존재론적 포지셔닝** | `backend/app/routers/ai_suggestion.py` Gemini 프롬프트 — "당신은 축구 코치입니다 … 감독을 도우세요" | AI가 결정을 대신 내리는 것이 아니라 "감독(사용자)을 보조하는 코치"로 명시적으로 역할 규정 — 최종 결정권은 항상 사용자(감독)에게 있다는 설계 원칙을 프롬프트 레벨에서부터 못박음 |
| **"전술적 결함" 진단 문구** | `model/app/predictors/win_predictor/tactics_*.py`가 생성하는 한국어 페널티 문구(예: "폭을 넓히는 전술인데 윙어/사이드 자원이 부족함") | 단순 수치가 아니라 감독이 회의실에서 듣는 듯한 자연어 코멘트로 변환해, "데이터 분석관이 감독에게 보고하는" 톤을 재현 |
| **더그아웃/대시보드 톤의 UI** | `styles/global.css` — 다크 글래스모피즘, BBC 3D 스타일 피치, 방송 그래픽풍 승률 바 | "게임 화면"이 아니라 "감독이 보는 전술 분석 콘솔"처럼 보이도록 전체 톤을 통일 |

### 2.3 "감독 경험"이 사용자 결정 범위를 규정한다

- 승률 계산은 **버튼을 눌렀을 때만** 1회성으로 실행되고 실시간 자동 재계산이 없다 — 이는 "매 순간 조작하는 플레이어 경험"이 아니라 "지시를 확정하고 결과를 확인하는 감독 경험"을 의도적으로 구현한 제약이다(`setting.md`에 "경기 중 라이브 시뮬레이션이 아니라 프리매치 스쿼드/전술 편집 도구"라고 명시).
- 실경기(토너먼트 라이브 매치)에서도 사용자가 매 순간 개입하는 게 아니라, **하이드레이션 브레이크 단위(정규시간 4구간 + 필요 시 연장 2구간)로만 개입 지점이 열린다** — 이는 실제 감독이 경기 중 벤치에서 할 수 있는 일(하프타임/음료타임 지시 변경, 교체)과 대응되도록 설계된 것으로, "선수를 조종"하는 게 아니라 "지시를 갱신"하는 감독의 권한 범위를 그대로 게임 규칙화한 것이다.
- 벤치의 "프라임 선수(전성기 레전드)" 소환 기능도 임의 선수를 아무 자리에나 넣을 수 없고, **원 소속 선수의 슬롯에만, 포지션이 맞을 때만** 교체 가능하도록 제한된다 — "감독이 실제로 행사할 수 있는 인사권"의 범위를 게임 규칙으로 못박은 것.

### 2.4 설계는 됐으나 아직 구현되지 않은 "감독 경험" 확장 지점

- **감독 유형(4~8종) 판정 로직 + 공유 카드** — `frontend/README.md`에 이미 기획돼 있으나 미구현. 세션 동안 사용자가 실제로 선택한 전술 성향(공격적/실리적/점유율 지향 등)을 분석해 "당신은 OO형 감독입니다" 식으로 유형화하고 공유 가능한 결과 카드를 주는 기능. 성격유형 테스트류의 정체성 부여 메커닉을 감독 경험에 접목하려는 의도.
- **더그아웃 상주형 AI 코치 캐릭터** — 병합되지 않은 `front` 브랜치에는 모자를 쓴 코치 캐릭터가 화면에 상시 떠 있다가 말풍선으로 조언하는 `CoachButton.jsx` 컴포넌트가 존재했다(현재 `dev`에는 없음). 최종적으로는 분석 탭 안의 정적 패널(`AiSuggestionPanel.jsx`)로 단순화되어 배포됐지만, 라이브 매치 대시보드에는 여전히 "전술 코치: 추후 제공 예정" placeholder 카드가 남아 있어, **AI 코치를 분석 화면뿐 아니라 경기 전 구간에 상시 동행하는 캐릭터로 확장하려는 의도가 이어지고 있음**을 보여준다.

---

## 3. 페이지 구성

앱은 라우터 라이브러리 없이 `App.jsx`의 단일 뷰 상태(`'landing' | 'teamSelect' | 'board' | 'tournament'`)로 화면을 전환한다. 전역 게임 상태는 `GameContext`(Context + useReducer)가 관리한다.

```
Landing(랜딩)
   └─▶ TeamSelect(팀 선택)
          └─▶ Tournament(토너먼트, 8강 브래킷)
                 │  "전술 수정" 클릭 시 ↕ (Tournament는 언마운트되지 않고 숨김 처리되어 진행 상태 보존)
                 ▼
              TacticalBoard(전술 보드)
                 ├─ [포메이션] 서브뷰: Sidebar + 3D PitchBoard + PlayerCard/BenchList
                 └─ [분석] 서브뷰: TeamAnalyticsBoard (레이더차트/선수비교/AI 전술 제안)
```

| # | 페이지/뷰 | 핵심 구성 요소 | 역할 |
|---|---|---|---|
| 1 | **Landing** | 브랜드 타이틀·이거비·"시작하기" CTA | 진입점, 브랜드 각인 |
| 2 | **TeamSelect** | 국기 그리드(내 팀/상대 팀), 팀 설명 패널(플레이스타일+기본 포메이션), "토너먼트 시작" 버튼 | 대회에 참가할 내 팀 결정 |
| 3 | **TacticalBoard – 포메이션 서브뷰** | 좌측 `Sidebar`(포메이션/공격/수비/조율 4탭), 중앙 3D `PitchBoard`(카메라 3종), 우측 `PlayerCard`+`BenchList`(프라임 선수 토글), 상단 `WinProbabilityPanel` HUD | 라인업·전술 편집의 메인 작업대 |
| 4 | **TacticalBoard – 분석 서브뷰**(`TeamAnalyticsBoard`) | `TeamAnalysisPanel`(공수 레이더차트, 강점/약점, 전술적 결함 칩, `AiSuggestionPanel`), `PlayerComparePanel`(선수 1:1 비교) | 현재 세팅의 진단과 개선 아이디어 확인 |
| 5 | **Tournament** | `BracketLines` SVG 대진표(8강→4강→결승/3·4위전), 경기 카드, 경기 상세/골 타임라인 모달 | 대회 진행 관리, 라운드 간 전술 유지/재조정 허브 |
| 6 | **LiveMatchDashboard**(Tournament 내부 모달) | `PitchZoneMap`+`PassZoneMap`(공간 장악), `StatTornadoRow` 그룹(지표 대결), `RadarChart`(팀 스타일), `StatMomentumSpark`(모멘텀), "전술 코치" placeholder | 유저 경기를 구간 단위로 관람·점검 |

---

## 4. 핵심 인터랙션 명세

| 인터랙션 | 트리거 | 동작 | 결과/제약 |
|---|---|---|---|
| **팀 슬롯 스왑** | TeamSelect에서 이미 선택된 국기를 다시 클릭 | 내 팀/상대 팀 슬롯이 서로 교체됨 | 같은 팀 중복 선택 불가 |
| **포메이션 자동 배치** | Sidebar [포메이션] 탭에서 프리셋 클릭 | `assignFormation()`이 포지션 적합도(정확 일치 100점/인접 포지션 40점) + 스탯을 기준으로 그리디 최적 배치 수행 | 클릭 한 번으로 11명 전원 재배치, "기본값 복원" 버튼으로 되돌리기 가능 |
| **선수 위치 교체** | 3D 피치 위 선수 클릭 → 다른 슬롯/벤치 선수 클릭 | 두 선수의 포지션이 맞바뀜 | GK는 GK와만 교체 가능 |
| **프라임 선수 소환** | BenchList의 "⭐ 프라임 선수" 토글 → 레전드 카드 클릭 | 원 소속 선수의 슬롯에만 교체 삽입(포지션 고정) | 원본 선수와 프라임 버전 동시 출전 불가, 포지션 불일치 시 비활성화+사유 툴팁 |
| **전술 다이얼 조작** | Sidebar [공격]/[수비]/[조율] 탭 슬라이더·토글·셀렉트 | `tacticConfig`의 해당 필드가 즉시 갱신 | 자동 재계산 없음 — 승률 갱신은 별도 버튼으로만 |
| **카메라 프리셋 전환** | PitchBoard 상단 버튼(중계 캠/**감독 시점**/탑뷰) | Three.js 카메라가 해당 프리셋으로 부드럽게 보간 이동, 감독 시점은 더그아웃 피규어 눈높이 1인칭 + 유휴 헤드밥 | 3종 모드 어디서든 확대/축소 가능 |
| **승률 계산** | "승률 계산" 버튼 클릭(수동, 1회성) | `POST /api/win-probability` 1회 호출 → `WinProbabilityPanel`의 승/무/패 바 갱신 | 직전 계산 결과와 비교한 %p 델타·화살표를 함께 표시 |
| **AI 전술 제안** | 분석 탭의 "AI 전술 제안(Gemini)" 버튼 | 현재 포메이션/전술/스탯/승률/전술적 결함을 종합한 프롬프트로 Gemini 호출 | 승률을 최소 1회 계산하기 전에는 버튼 비활성화, 로딩/에러/"다시 요청" 상태 지원 |
| **선수 1:1 비교** | 분석 탭 `PlayerComparePanel`에서 내 팀·상대 팀 선수 드롭다운 선택 | 두 선수의 사진 + 레이더차트 + 전체 스탯 비교표 표시 | 포지션 무관하게 임의 두 선수 비교 가능 |
| **토너먼트 라운드 진입/전술 재조정** | Tournament에서 다음 상대 확인 후 "전술 수정" 클릭 | `TacticalBoard`로 이동(직전 라인업/전술 유지), 조정 후 "토너먼트로" 버튼으로 복귀 | Tournament는 언마운트되지 않고 숨김 처리되어 브래킷/진행 상태 보존 |
| **유저 경기 구간 진행** | Tournament에서 내 경기 시작 | 정규시간을 4개 하이드레이션 브레이크 구간으로 나눠 순차 진행, 구간 사이 일시정지 모달에서 `LiveMatchDashboard` 관람 + 필요 시 "전술 수정"으로 재조정 가능 | 무승부 시 연장 2구간 추가, 그래도 무승부면 고정 75% 성공률의 승부차기로 종료 |
| **대진표 연결선 렌더링** | 브래킷 화면 로드/리사이즈 | `BracketLines`가 실제 경기 카드의 DOM 좌표를 측정해 SVG 연결선을 그림(`ResizeObserver`로 반응형 유지) | 8강→4강→결승 계층 구조를 시각적으로 연결 |

---

## 5. 데이터 활용 방식

### 5.1 3계층 책임 분리 아키텍처

```
frontend (Vite+React19+Three.js, 5173)   — 사용자 입력·3D 렌더링만, 계산 없음
   │ 브라우저 → backend, HTTP
   ▼
backend (FastAPI, 4000)                   — seed 데이터 조회/정제 + model 프록시, 계산 없음
   │ 서버 → 서버, Docker 네트워크
   ▼
model (FastAPI, 8000)                     — 순수 계산 전용, 자체 데이터 저장 없음
```

- 매 계산 요청마다 라인업 전체·전술 5섹션 전체·선수 오버라이드가 **원본 그대로** frontend → backend → model까지 전달된다. 실제 feature 변환/추론은 model 내부의 `predictors/*.py`에서만 일어나므로, 이후 로직을 통째로 교체해도 backend/frontend 코드를 건드릴 필요가 없도록 설계돼 있다.
- DB는 없다 — 모든 시드 데이터는 Python 모듈 레벨의 인메모리 리스트(`TEAMS`, `FORMATIONS`, `TEAM_TACTIC_PRESETS`, `PRESETS` 등)이며, 새로고침하면 세션 상태가 초기화된다.

### 5.2 시드 데이터 (backend/app/data)

| 데이터 | 내용 | 비고 |
|---|---|---|
| 팀/로스터 | 한국(26명)·남아공(26명), 실제 2026 월드컵 공식 명단 기준 이름/포지션/나이/키, 감독명(홍명보/Hugo Broos) | 10종 스탯·양발 능력치는 사람이 임의 배정한 값(실제 스카우팅 데이터 아님) |
| 포메이션 | 백3/4/5 기반 22종 | 좌표 없이 역할 목록만 제공, 좌표 변환은 frontend `formationLayout.js`가 담당 |
| 팀 전술 프리셋 | 팀별 기본 포메이션 + 선발 11명 + `TacticConfig` | 팀 선택 시 초기값으로 로드 |
| 프라임(레전드) 선수 프리셋 | 손흥민 전성기, 메시, 호날두 등 실존 선수의 전성기 버전 | 손흥민만 실제 튜닝값, 나머지는 전 스탯 90 placeholder |
| 골 시나리오 | — | 스키마/라우터만 존재, 데이터 비어있음(미사용 기능) |

### 5.3 계산 로직 (model, 현재는 학습된 ML이 아닌 손튜닝 휴리스틱)

- **승률 예측(`win_predictor`)**: 선수별 10종 스탯을 포지션별 가중치로 합산(공격수는 결정력, 미드필더는 패스/시야, 수비수는 태클/마크 가중)해 팀 베이스 레이팅을 만들고, 포지션 미스매치·평균 연령·평균 신장에 대한 페널티를 적용한다. 이후 전술 5섹션 각각이 팀의 실제 능력치와 맞는지 검사해 시너지(+)나 결함(−)을 가감하며, 결함은 "폭을 넓히는 전술인데 윙어 자원이 부족함" 같은 한국어 문구로도 기록돼 분석 탭의 "전술적 결함" 칩과 AI 코치 프롬프트에 재사용된다. 최종적으로 두 팀 레이팅 차이를 tanh로 강도화한 뒤 승/무/패 확률로 변환한다(격차가 클수록 무승부 확률이 축소).
- **경기 시뮬레이션/통계(`match_predictor`)**: 동일한 팀 레이팅을 바탕으로 기대 득점(λ)을 산출해 포아송 분포로 스코어·득점자·득점 시각을 샘플링하고, 점유율·구역별 침투·슛·패스·파울 같은 경기 통계도 팀 레이팅과 전술 설정에서 파생된 평균값 주변에서 포아송 샘플링으로 생성한다. 실제 이벤트 로그가 아니라 통계적으로 일관되게 생성된 값이다.
- **AI 전술 제안**: 포메이션명·양 팀 전술 5섹션 전체·선발/벤치 스탯·현재 승/무/패 확률·전술적 결함 코드를 모두 하나의 한국어 프롬프트에 담아 Gemini에 전달하고, 응답 텍스트를 그대로 노출한다.
- model에는 `scikit-learn`/`joblib`이 이미 설치돼 있고, 각 predictor에 `_load_model()` 스텁이 마련돼 있어 — 향후 실제 학습된 모델(`.joblib`)을 넣고 이 스텁만 채우면 위 휴리스틱을 손 안 대고 교체할 수 있도록 설계돼 있다.

---

## 6. 주요 사용 흐름

1. **랜딩** — 사용자가 "시작하기"를 눌러 진입.
2. **팀 선택** — 내가 이끌 국가대표팀과 첫 상대(사실상 토너먼트 시드)를 선택. 팀 설명 패널에서 기본 플레이스타일/포메이션을 확인.
3. **토너먼트 개시** — "토너먼트 시작" 클릭 시 8강 대진표가 구성되며, 내 팀은 항상 8강 1경기에 배정된다.
4. **경기 전 전술 설계** — 다가오는 상대가 확정되면 "전술 수정"으로 전술 보드에 진입. [포메이션] 탭에서 포메이션 프리셋 적용 또는 수동 스왑으로 라인업을 짜고, 필요 시 프라임 선수를 소환해 전력을 보강한 뒤, [전술] 탭에서 5개 섹션의 세부 다이얼을 조정한다.
5. **사전 검증** — "승률 계산" 버튼으로 현재 세팅의 예측 승/무/패를 확인하고, 이전 시도 대비 %p 변화를 본다. 분석 탭에서 공수 레이더차트·강점/약점·"전술적 결함" 칩을 확인하고, AI 전술 제안(Gemini)을 요청해 개선 아이디어를 참고한 뒤 다시 전술을 조정한다.
6. **토너먼트 복귀 및 경기 시작** — "토너먼트로" 버튼으로 복귀(다른 대진 경기들은 이 사이 즉시 자동 시뮬레이션됨). 내 경기를 시작하면 정규시간이 4개 구간으로 나뉘어 진행되고, `LiveMatchDashboard`에서 공간 장악·지표 대결·모멘텀 추이를 구간마다 확인한다.
7. **구간 중 재조정** — 각 구간 사이 일시정지 시점에 "전술 수정"으로 전술 보드에 다시 진입해 흐름을 보고 지시를 바꿀 수 있다(선수를 직접 조작하는 것이 아니라 지시를 갱신하는 개입).
8. **연장/승부차기** — 정규시간 무승부 시 연장 2구간이 추가되고, 그래도 갈리지 않으면 고정 확률 승부차기로 승부를 가른다.
9. **라운드 반복** — 경기 종료 후 승리하면 내 팀의 라인업/전술 세팅을 유지한 채 다음 라운드(4강→결승/3·4위전)로 자동 진행되며, 4~8단계를 라운드마다 반복한다.
10. **대회 종료** — 우승(또는 탈락) 결과 화면으로 마무리. 이번 실행 동안 내린 전술적 결정들이 처음(랜딩~팀 선택)부터 끝(우승/탈락)까지 하나의 "감독으로서의 시즌"을 이루는 서사로 완결된다.
